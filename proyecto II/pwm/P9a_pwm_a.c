#include "servir.h"

void main() {
  InitMain();

  while (1) {                         // endless loop
  servir('p');                        // p => pequeño , m => mediano , g => grande, x => extra grande (tamaño de la mascota)
  }
}